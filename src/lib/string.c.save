#include <stddef.h>
#include "string.h"

void *memcpy(void *s1, const void *s2, size_t n)
{
	size_
	for (i = 0 ; i < n ; i++)
	{
		*s1 = *s2;
	}
	return s1;
}

size_t strlen(const char* str)
{
	size_t i = 0;
	while(str[i] != 0)
		i++;
	return i;
}
